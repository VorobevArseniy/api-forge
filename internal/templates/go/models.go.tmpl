// Code generated by api-generator. DO NOT EDIT.
package api

{{ range $svcName, $svc := .Services }}
    {{ range $epName, $ep := $svc.Endpoints }}
        // {{ $epName }}Request - входные данные для {{ $svcName }}.{{ $epName }}
        type {{ $epName | ToTitle }}Request struct {
            {{- range $field, $type := $ep.Request }}
            {{ $field | ToPascal }} {{ $type | ToGoType }} `json:"{{ $field | ToSnake }}"`
            {{- end }}
            {{- if $ep.Query }}
            // Query-параметры
            {{- range $param, $type := $ep.Query }}
            {{ $param | ToPascal }} {{ $type | ToGoType }} `json:"-" url:"{{ $param }}"`
            {{- end }}
            {{- end }}
        }

        // {{ $epName }}Response - ответ от {{ $svcName }}.{{ $epName }}
        type {{ $epName | ToTitle }}Response struct {
            {{- range $field, $type := $ep.Response }}
            {{ $field | ToPascal }} {{ $type | ToGoType }} `json:"{{ $field | ToSnake }}"`
            {{- end }}
        }
    {{ end }}
{{ end }}
